:root{
  --bg0:#070a0f;
  --bg1:#0b0f14;
  --panel:#121a25;
  --ink:#e9eef7;
  --muted:rgba(233,238,247,.72);

  --parchment:#efe1c8;
  --parchment2:#e7d5b6;
  --parchInk:#2a241f;

  --copper:#b98b55;
  --green:#7fd1b9;

  --shadow: 0 18px 50px rgba(0,0,0,.55);
  --radius: 22px;

  /* portrait signage sizing */
  --maxW: 1000px; /* safe for 1080px wide portrait */
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  background: #000;
  color: var(--ink);
  font-family: ui-serif, Georgia, "Times New Roman", Times, serif;
  overflow:hidden; /* no scroll */
}

.stage{
  position:relative;
  height:100%;
  width:100%;
  display:grid;
  place-items:center;
  padding: 24px;
}

.bg{
  position:absolute;
  inset:0;
  background:
    radial-gradient(1200px 1600px at 50% 35%, rgba(127,209,185,.10), transparent 65%),
    radial-gradient(900px 1200px at 50% 110%, rgba(185,139,85,.10), transparent 55%),
    linear-gradient(180deg, var(--bg0), var(--bg1) 55%, #06080c);
  filter:saturate(1.05);
}

/* canvases */
#steam, #embers{
  position:absolute;
  inset:0;
  pointer-events:none;
}

.frame{
  position:absolute;
  inset: 18px;
  border-radius: 28px;
  pointer-events:none;
}

/* inner frame glow + faux bevel */
.frame-inner{
  position:absolute;
  inset:0;
  border-radius: 28px;
  box-shadow:
    0 0 0 2px rgba(185,139,85,.25),
    0 0 40px rgba(185,139,85,.10),
    inset 0 0 0 1px rgba(255,255,255,.05),
    inset 0 0 60px rgba(0,0,0,.35);
}

/* Rune ring sits outside menu content */
.rune-ring{
  position:absolute;
  inset: 0;
  filter: drop-shadow(0 0 14px rgba(127,209,185,.20)) drop-shadow(0 0 26px rgba(185,139,85,.14));
  animation: ringFloat 8s ease-in-out infinite;
}
@keyframes ringFloat{
  0%,100%{transform: translateY(0px)}
  50%{transform: translateY(-6px)}
}

.rune{
  position:absolute;
  font-family: ui-serif, Georgia, serif;
  font-size: 26px;
  color: rgba(127,209,185,.60);
  text-shadow:
    0 0 10px rgba(127,209,185,.35),
    0 0 22px rgba(185,139,85,.20);
  animation: runePulse 2.8s ease-in-out infinite;
  opacity:.85;
}
@keyframes runePulse{
  0%,100%{transform: translate(-50%,-50%) scale(1); opacity:.78}
  50%{transform: translate(-50%,-50%) scale(1.06); opacity:1}
}

/* rune positions around a ring */
.r1  {left:50%; top:6%}
.r2  {left:72%; top:10%}
.r3  {left:88%; top:22%}
.r4  {left:94%; top:42%}
.r5  {left:88%; top:62%}
.r6  {left:72%; top:76%}
.r7  {left:50%; top:84%}
.r8  {left:28%; top:76%}
.r9  {left:12%; top:62%}
.r10 {left:6%;  top:42%}
.r11 {left:12%; top:22%}
.r12 {left:28%; top:10%}

.menu{
  position:relative;
  width:min(var(--maxW), 100%);
  height: calc(100% - 30px);
  display:flex;
  flex-direction:column;
  gap: 16px;
  padding: 18px 18px 14px;
}

/* header */
.menu-header{
  text-align:center;
  padding-top: 8px;
}
.brand{
  font-size: 56px;
  letter-spacing:.5px;
  line-height:1;
}
.subtitle{
  font-size: 22px;
  opacity:.9;
  margin-top: 6px;
  color: rgba(233,238,247,.86);
}
.note{
  margin-top: 8px;
  font-size: 16px;
  color: rgba(233,238,247,.70);
}

/* main grid: two columns (drinks left, food right) */
.grid{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
  flex: 1;
  min-height: 0;
}

/* parchment cards */
.card{
  background: linear-gradient(180deg, var(--parchment), var(--parchment2));
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  color: var(--parchInk);
  padding: 14px 14px 12px;
  position:relative;
  overflow:hidden;
}
.card:before{
  content:"";
  position:absolute;
  inset:0;
  background:
    radial-gradient(600px 300px at 30% 15%, rgba(0,0,0,.07), transparent 60%),
    radial-gradient(700px 380px at 80% 90%, rgba(0,0,0,.09), transparent 65%);
  mix-blend-mode:multiply;
  opacity:.65;
  pointer-events:none;
}
.card h2{
  margin: 0 0 10px;
  font-size: 28px;
  letter-spacing:.2px;
  text-align:center;
}
.group{
  display:flex;
  flex-direction:column;
  gap: 10px;
}
.pill{
  background: rgba(255,255,255,.55);
  border: 1px solid rgba(0,0,0,.10);
  border-radius: 18px;
  padding: 10px 12px;
  text-align:center;
}
.pill .title{
  font-size: 22px;
  line-height:1.15;
}
.pill .desc{
  font-size: 16px;
  opacity:.85;
  margin-top: 6px;
  line-height:1.25;
}
.row{
  display:flex;
  justify-content:space-between;
  align-items:baseline;
  gap: 10px;
}
.price{
  font-variant-numeric: tabular-nums;
  font-weight: 700;
  font-size: 20px;
  opacity:.9;
  white-space:nowrap;
}

/* compact lists */
.list{
  margin: 0;
  padding: 0;
  list-style:none;
  display:flex;
  flex-direction:column;
  gap: 8px;
}
.list li{
  background: rgba(255,255,255,.45);
  border: 1px solid rgba(0,0,0,.10);
  border-radius: 16px;
  padding: 10px 12px;
}
.list .name{
  font-size: 20px;
  line-height:1.15;
}
.list .meta{
  margin-top: 6px;
  font-size: 15px;
  opacity:.82;
  line-height:1.25;
}

.menu-footer{
  text-align:center;
  padding-bottom: 6px;
}
.tiny{
  font-size: 14px;
  color: rgba(233,238,247,.62);
}

/* Portrait TVs vary—keep readable */
@media (max-width: 900px){
  .brand{font-size: 46px}
  .grid{gap: 12px}
  .card h2{font-size: 24px}
  .pill .title{font-size: 20px}
  .price{font-size: 18px}
}

/* ultra small fallback */
@media (max-width: 700px){
  body{overflow:auto}
  .stage{padding: 10px; overflow:auto}
  .grid{grid-template-columns: 1fr}
}